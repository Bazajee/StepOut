import express from 'express'
const path = require('path');

const app = express()

const facts =  
app.get('/fact', function(request, response) {
        response.render('profile', {
        
        })
     })

     app.get('/monument', function(request, response) {
        response.render('profile', {
        
        })
     })

     app.get('/poi', function(request, response) {
        response.render('profile', {
        
        })
     })